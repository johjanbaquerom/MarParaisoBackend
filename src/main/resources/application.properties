# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/mar_paraiso
spring.datasource.username=root
spring.datasource.password=C123456.

# Configuración del pool de conexiones (HikariCP, por defecto en Spring Boot)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Logs para SQL (opcional)
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace

spring.resources.static-locations=classpath:/static/, file:./uploaded-images/
